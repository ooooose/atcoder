{
  "version": "0.2.0",
  "configurations": [
      {
          "name": "(LLDB) Debug",
          "type": "lldb",
          "request": "launch",
          "program": "${fileDirname}/${fileBasenameNoExtension}",
          "args": [],
          "cwd": "${fileDirname}/",
          "preLaunchTask": "g++ build active file",
      }
  ]
}
